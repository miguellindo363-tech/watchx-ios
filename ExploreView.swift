import SwiftUI

struct ExploreView: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("Explorar").font(.title2).bold()
            Text("Loja de watchfaces (em desenvolvimento)")
        }.padding()
    }
}
